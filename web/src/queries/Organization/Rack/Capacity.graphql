query Capacity($oid: ID!, $rid: ID!) {
  organization(id: $oid) {
    id
    rack(id: $rid) {
      id
      capacity {
        cpu {
          total
          used
        }
        mem {
          total
          used
        }
      }
    }
  }
}
