query updates($oid: ID!, $rid: ID!) {
  organization(id: $oid) {
    id
    rack(id: $rid) {
      id
      updates {
        created
        id
        parameters {
          key
          value
        }
        version
      }
    }
  }
}
