query Runtime($oid: ID!, $id: ID!) {
  organization(id: $oid) {
    id
    runtime(id: $id) {
      id
      engines {
        description
        name
      }
      provider
      regions
    }
  }
}
